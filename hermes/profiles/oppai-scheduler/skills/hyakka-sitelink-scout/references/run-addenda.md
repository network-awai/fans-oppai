# hyakka sitelink-scout: run addenda (overflow file)

## 2026-09-18 run — PR #1021 OPEN (840b2581 on 5a153777, +61, evidence 2967→2977 exact, gate 10/10)
- The 09-17 no-land residue LANDED: Q1764 Reykjavík 216, Q2088/Q23397/Q34969 215, Q724/Q759/Q7026/Q12143/Q100/Q11629 215 — all re-verified live, byte sizes == gate's. Paintings Q12418/Q45585 were NOT re-fetched this run (215-band residue filled the pool before the walk reached them; carry continues).
- No discovery query burned: adopted QLever 20260909 ranking, walked ranks 1650-1870 (band 215; ~180 rows, most refusals day-of-year Q14795564 + colon-junk + year-class). Fresh KEEPs outranked by nothing but pool-size: Q303 Elvis Presley 215, Q2089 1998, Q49622 2023, Q4035 Satyajit Ray, Q1371 West Virginia, Q19860 Indo-European, Q2934 goat, Q2475 1971 + others carried in /tmp/sl-walk-carry-20260918.json (resume rank 1870).
- ONE open PR (#1020, 0 EntityData claims). Splice into bare-] blank-tolerant tail, 61 lines, +61/−0. Evidence temp-worktree base 2967 → branch 2977, per-id greps 2x line-anchored.
- NEXT RUN: discovery query OVERDUE (ranking 9 days old). Fresh QLever GET-with--L (SPARQL 502s/5-min timeouts observed 09-17). If budget blocks the rebuild, walk /tmp/sl-walk-carry-20260918.json residue first and open no PR.
- Generator tail (calibrated this run): maps joined with ',\n', last line '...:llm? true}]}' 3-glyph fused; assert SEMANTIC counts (fused==1, standalone 'true}' or 'true},', :id-line count==len(picks)) — positional bl[-2]/[-3] asserts false-fire because of the comma suffix.

## 2026-09-17 run — NO LAND (run budget exhausted between gate and landing); PHANTOM-DIRTY refusal recovery

- Pre-run REFUSED ('local changes to config/knowledge-ingest.edn would be overwritten by checkout') — phantom-dirty AGAIN: tracked porcelain + diff + lsof all empty; plain retry of the fetch+detach succeeded first try (worktree .gftd/worktrees/hyakka-commons-bot, detached 8e267a5e2 == origin/main).
- Scripts renamed .cljs → .cljk on main (verify_source_proposal.cljk, wiki_growth_evidence.cljk) — update any hardcoded .cljs path. Gate classpath that passed: src + globbed ../../kotoba-lang/*/src + gitlibs kotoba-lang/text 73bdb13a/src.
- 0 open PRs (limit-300, verified). Exclusion set = live config alone: 1513 EntityData QIDs + 32 book:qids QIDs = 1545.
- NO discovery query burned (one-query budget intact). Walked ranks 1602-1649 of tmp-sl-ranked-20260909.json in one fast segment (~2 min): 14 KEEP (residue-first Q1764/Q2088 re-verified live), refusals: 15 ref-p31 ALL day-of-year Q14795564 ('May 29'-class rows), 4 ref-colon, 1 cfg (Q3783), 0 fetchfail.
- Gate 10/10 exit 0 pre-landing (byte sizes == own fetches): Q1764 Reykjavík 216, Q2088 (1997) 216, Q23397 lake 216, Q34969 Benjamin Franklin 216, Q724 Maine 216, Q759 New Hampshire 216, Q7026 Catalan 216, Q12143 time zone 215, Q100 Boston 215, Q11629 painting 215.
- RUN BUDGET EXHAUSTED before branch/splice/evidence/PR — NOTHING LANDED. Next run must: re-grep live cfg + PR sweep, re-verify these 10 from EntityData (or re-gate), then land from /tmp/sl_picks.json (entity JSONs also cached at /tmp/sl_ent_<QID>.json). Proposal file: /tmp/sl_prop_0917.edn (gate-approved shape: 9 standalone true} lines + fused true}]}, no trailing blank).
- NEW generator bug pattern (caused a double-close assert abort): the proposal template's trailing ']}' appended AFTER a fused last-map line yields braces 11/12 — the fused close must REPLACE the template tail, not ride beside it. When the assert aborts AFTER a healthy earlier file write, the gate may pass on the OLD file — always regenerate + re-run STRUCT before trusting a gate result. Walk residue (KEEP, unclaimed): Q1371 West Virginia 215, Q2934 goat 215, Q19860 Indo-European 215, Q2475 (1971) 215; next walk resumes rank 1650. Fresh QLever discovery query due within ~1-2 runs (tail cuts 176, band descending through 215).

## 2026-09-10 run (post-1502) — REFUSED pre-run, report-and-stop

- Pre-run evidence collector exited 1: nbb `Could not find namespace: kotoba.lang.text` — the collector runs the plain `--classpath src` shape that run-1501's addendum already fixed for the bot's own gate/evidence (deps.edn :paths + gitlibs kotoba-lang/text src). Contract followed: report-and-stop; no proposal, no PR, no walk; one-query budget untouched.
- Run state UNCHANGED from run-1502: walk tmp-sl-ranked-20260909.json from rank 1602, residue Q1764 Reykjavík 216 / Q2088 1997 216 (re-verify live), #958 OPEN, paintings Q12418/Q45585 carried.
- Until the collector gets the run-1501 classpath fix, every run will refuse pre-run with this same error — surface the collector fix to the user; do not work around the REFUSED contract within a run.

## 2026-09-10 run 1502 (PR #966) addenda

- WORKTREE-REGISTRATION TRAP: `git worktree remove` for /tmp base worktrees must run with cwd=THE BASE WORKTREE ITSELF (its gitdir owner), not the growth worktree — from the growth tree it silently no-ops (rc 0) and every land cycle re-used a STALE base worktree pinned to the old sha, corrupting the evidence delta (1 not 10). Hard-delete fallback: literal-path `rm -rf /tmp/sl<run>/base_wt` + `git worktree prune` (never parameterized).
- FOURTH TAIL SHAPE seen at base e6ebfb1e6: vector close is a BARE `]` line, then BLANK, then top-level `}` — a naive scan-back lands on the `}` line and rc7s. Insert-point finder must: scan back over blanks to the LAST non-blank; if it is the top-level `}` alone, scan back over blanks AGAIN to the vector-close line and insert there (preserves the trailing blank + `}`). Shapes enumerated: bare-] / bare-]-then-top / standalone-]} / close-]}-then-top / fused-}-then-top.
- THE DELTA-10 ASSERT WORKS AS A MOVE DETECTOR: first evidence run showed delta 1 (not 10) and exposed a mid-run main move (#957 +1 OFAC non-EntityData map) that the stale base had masked. Never soften `c_sources == b_sources + 10` to an inequality.
- Paintings Q12418/Q45585 carried a SIXTH time — the 217-216 walk band outranks 146/76 again; they enter the pool only when the walk band drops below 146.
- Post-create: main moved again; local `git merge origin/main` + push `HEAD:refs/heads/<BR>` (ls-remote==local) + multiset id diff (branch−main == exactly my 10, main−branch == ∅) + merged evidence 2812 (= 2810 + 1 absorbed non-EntityData map) exit 0, per-id greps 2x — all green; PR MERGEABLE/CLEAN.
- Walk: ranks 1555-1601 of tmp-sl-ranked-20260909.json, 12 KEEP in 47 rows (32 ref-p31 day-of-year Q14795564, 3 ref-colon, 0 fetchfail, 0 cfg/PR overlap). Picks (EntityData-verified, band 217-216): Q1214 Wyoming 217, Q5705 Catalonia 217, Q5372 basketball 217, Q2091 1999 217, Q5465 Cape Town 217, Q2062 1991 217, Q9036 Nikola Tesla 217, Q1533 Caracas 216, Q1454 North Carolina 216, Q1212 Montana 216. Walk residue (KEEP, unclaimed): Q1764 Reykjavík 216, Q2088 1997 216.
- Next run: residue-first Q1764/Q2088 (re-verify live), then walk tmp-sl-ranked-20260909.json from rank 1602. Paintings enter when band < 146. Fresh discovery query due within ~1-2 runs (tail cuts 176; the 216 band sits ~250 rows above the cut).

## 2026-09-09 run 1501 (PR #958) addenda

- WRONG-WORKTREE PICKER BUG: first bootstrap chose worktrees by ranking-file existence only and landed on the UNRELATED 'amu' repo (.gftd/worktrees/amu happens to hold a tmp-sl-ranked file) — its quarantine-on-detach then moved two of amu's untracked files (restored immediately). Fix: filter candidates by `git remote get-url origin` containing 'app-hyakka' BEFORE any destructive step.
- nbb classpath rot now bites the SITELINK bot too: plain `--classpath src` gate dies `Could not find namespace: kotoba.lang.text` (09-09 fleet-wide rot, f3f29b22). Working classpath = deps.edn :paths (src:test + 14 relative ../../kotoba-lang/* source paths, all present in the .gftd checkout) + `$HOME/.gitlibs/libs/io.github.kotoba-lang/text/73bdb13ae7a3d004b44bca08be03a3191157a38f/src`. Gate 10/10 first try after the fix; evidence exit 0 with the same classpath. (.gftd/kotoba-lang/text/src also carries the ns — gitlibs path preferred per 0913a.) Extract :paths from origin/main:deps.edn, verify each dir exists, never hardcode.
- `git numstat` is NOT a command (my land abort AFTER splice+evidence, pre-commit): correct form `git diff --cached --numstat`. Recovery that held: verify on-disk geometry (maps 2791→2801, urls 1453→1463, id tokens x1, tail anatomy) → stage → numstat (+61/−0) → commit → push → ls-remote == HEAD.
- Residue re-grep discipline vindicated again: Q10987 honey + Q11059 Sanskrit (run-d2 residue) were ALREADY configured — siblings absorbed them; live config grep caught both pre-pool. #934 dropped-maps CONFIRMED absent from main and landed as picks 1-2: Q8514 desert 219, Q11035 telephone 218.
- Paintings Q12418 Mona Lisa 146 / Q45585 Starry Night 76 carried a FIFTH time — the 218-band walk outranks them again; they enter the pool only when the walk band drops below 146.
- Fresh 21st ranking: QLever GET-with--L answered 200 first-try (489KB bindings dict, 3000 rows, head Q136746190 935, tail cuts 176). Saved tmp-sl-ranked-20260909.json in hyakka-commons-bot worktree. Walk ranks 1300-1554: 12 KEEP in ~90 rows (band 219-218), 159 ref-p31 (Q14795564 wall), 31 ref-colon, 48 cfg, 0 fetchfail. Picks: Q8514 219, then Q11035/Q1069/Q11194/Q157/Q1435/Q5679/Q1987/Q8087/Q7809 all 218.
- Worktree this run: .gftd/worktrees/hyakka-commons-bot (growth worktree had no scratch); node_modules symlinked from the main checkout ~/github/com-junkawasaki/orgs/network-awai/app-hyakka/node_modules. No worktree carried node_modules or the ranking — probe before assuming.
- Label drift: batch comment + PR title say '20260910' but host clock (branch bot/sitelink-scout-20260909-1136) says 09-09 — cosmetic; identifiers are run-id 1501 + commit a11d023e.
- PR #958: OPEN MERGEABLE/CLEAN at verify, body read-back PASS (10 ids + QIDs + gate block + evidence 2796), ls-remote==HEAD. Next run: walk tmp-sl-ranked-20260909.json from rank 1555; fresh discovery query due within ~2-3 runs (tail cuts 176).

## 2026-09-09 run d (PR #930) addenda

- .gftd worktree (rotation back from .itonami-fleet). Fresh discovery query NOT burned: adopted run-sc3's 19th ranking tmp-sl-ranked-20260910-sc3.json (3000 rows, tail cuts 176). One-query budget intact.
- PR sweep: 1 open (#923 feed-scout ledger, 0 QID claims) then 2 (#926/#927, zero pick overlap). Residue-first 3/3 (Q1183 Puerto Rico 222 + paintings Q12418 146 / Q45585 76 — paintings again outranked BY the walk band, did NOT make top-10, carry a third time).
- Walk ranks 1340-1417 → 12 KEEP in 78 rows, band 222-220; 50 ref-p31 (day-of-year Q14795564 wall), 16 ref-colon, 0 fetchfail, 0 cfg/PR overlap. Picks: Q1183 222, Q702 Micronesia 222, Q779 Connecticut 222, Q9056 Czech 222, Q1603 Kentucky 221, Q3769 French Guiana 221, Q2042 Charles de Gaulle 221, Q383541 Basshunter 221, Q987 New Delhi 221, Q7378 elephant 221. Residue: Q10987 honey 220, Q11059 Sanskrit 220.
- MAIN MOVED THREE TIMES (5158ffe3→cbc9abb6→849eaf4e→d9609296, plus 721484a0 after create): pinned-sha rc3 aborts fired pre-write twice; a combined refresh+land cycle script (fetch→pin check→cfg/PR refresh+overlap→re-detach→re-gate→land when stable, 6-cycle cap) landed first-try on the cycle after the last pre-land move at 849eaf4e. Re-gate with identical byte sizes on every base.
- PROPOSAL-TAIL ASSERT variant (sc4 note disambiguated): the correct fused tail is THREE glyphs — map-close `}` + vector `]` + top `}` = `...true}]}`. Build the assert as ':llm? true'+chr(125)+chr(93)+chr(125); a two-glyph expectation false-fires on a healthy proposal (cost two pre-write aborts, zero side effects).
- Ancestor probe FALSE ALARM after PR create: merge-base --is-ancestor origin/main HEAD rc=1 only means main moved again, not that the branch is stale. Response: merge origin/main into the branch, push (fast-forward from remote tip), then poll gh pr view mergeable — GitHub is the operative signal, local ancestor checks are not.
- PR #930 went MERGED mid-poll via queue automation (mergedAt 17:10:35Z) while gh pr view headRefOid still showed a rewritten sha (757f63c0) and mergeable=UNKNOWN. AUTHORITATIVE landing check = mergeCommit.oid == origin/main + x1 id/url greps on the live main config (2686 maps / 1393 urls, all 10 mine x1, houjin x2 the only dupe).
- Landed #930: batch eeb64c6e (config-only +61/−0, maps 2674→2684, urls 1383→1393) on base 849eaf4e; gate 10/10 exit 0 on THREE bases (5158ffe3, cbc9abb6, 849eaf4e) with identical byte sizes == own fetches; evidence base 2669 → branch 2679 (+10 exact) exit 0, per-id greps 2x each; body read-back verified.
- process_manage wait false-exit (exited/null mid-run) recurred again; DONE-line + ps probe is the reliable wait. Patch tool failed on fresh /tmp content twice more — write_file full rewrite only.
- Next run: residue-first Q10987 honey 220 / Q11059 Sanskrit 220 (re-verify live), then walk tmp-sl-ranked-20260910-sc3.json from rank 1418; paintings Q12418/Q45585 enter the pool once the walk band drops below 146. Fresh QLever discovery query DUE (tail cuts 176, walk band descending through 220).

## 2026-09-09 run sc4 (PR #920) addenda

- .gftd worktree this run (rotation back from .itonami-fleet; resolve via cwd). git fetch over SSH can exceed 120s — bootstrap + every network-touching script via background terminal + process wait (foreground timeout kills mid-fetch, exit -15, silent).
- Fresh discovery query NOT burned: adopted run-sc3's 19th ranking tmp-sl-ranked-20260910-sc3.json (3000 rows, {rows,...} dict shape, head Q136746190 935, tail cuts at 176). One-query budget intact for next run.
- PR sweep 0 open PRs. Residue-first Q12418 Mona Lisa 146 / Q45585 The Starry Night 76 fetched clean (P31 Q3305213, colon 0.007/0.0) but ranked below the 223-band walk picks — STILL unclaimed, carry again.
- Walk ranks 1320-1339 → 12 KEEP in 20 rows, band 223-222; refusals 6 ref-p31 (day-of-year Q14795564 wall continues at this depth), 2 ref-colon (Module:Namespace detect + colon-frac 1.0), 0 fetchfail, 0 cfg/PR overlap. Picks (EntityData-verified): Q30875 Oscar Wilde 223, Q339 Pluto 223, Q7590 transport 223, Q7275 state 223, Q1354 Dhaka 222, Q1390 insect 222, Q1410 Gibraltar 222, Q1649 Oklahoma 222, Q11788 Ursidae 222, Q1221 Idaho 222. Walk residue: Q1183 Puerto Rico 222 (rank 1336, 11th of walk pool). Last rank 1340.
- Landing held the full guard chain: pinned-sha rc3 check (main quiet at 635effca), splice into bare-] blank-tolerant tail (61 batch lines, +61/-0 numstat, maps 2640→2650, urls 1363→1373), evidence base 2635 (temp worktree) → branch 2645 exit 0, gate 10/10 exit 0 with byte sizes == own fetches, per-id evidence greps 2x each, ls-remote==HEAD, PR #920 OPEN MERGEABLE/CLEAN, body read-back 1944 chars (10 ids + 10 QIDs + GATE_EXIT=0 + 2635/2645).
- Assertion-calibration bites (all cheap, asserts-first held): (1) a leftover walrus line set url_tok=None → re.escape(None) TypeError; (2) mkproposal tail assert expected fused 'true}]}' but the generator emits map-close line then fused close line — assert per-LINE (last non-empty == chr(93)+chr(125), prev line endswith ':llm? true}'), never endswith() on the whole body; (3) body read-back asserted 10 lowercase 'wikidata-qNNN' ids but the table-only body shape carries QIDs, not ids — after gh pr edit added a '### Source ids' section, read-back passes 10/10 ids + 10/10 QIDs; calibrate token checks to the body's actual vocabulary.
- Next run: residue-first Q1183 Puerto Rico 222 + paintings Q12418/Q45585 (all EntityData-verified this run, re-verify live), then walk tmp-sl-ranked-20260910-sc3.json from rank 1340. Ranking fresh 09-08; discovery query DUE (tail cuts at 176, walk band descending through 222).

## 2026-09-08 run sc3 (PR #914) addenda

- JST 22:26-23:28. Worktree was ~/.itonami-fleet/worktrees/hyakka-sitelink-bot (NOT .gftd — resolve via cwd, never hardcode). Stdout capture dead as usual; every step self-logged via wrappers `exec > /tmp/sl_run_sc3/<stage>_wrap.log` + python file scripts run with `python3 -I` (NEVER `zsh script.py` — silent noop, run-jj trap).
- FRESH DISCOVERY QUERY (19th ranking): WDQS bypassed (per skill), QLever POST answered HTTP 308 x2 then GET-with-curl-L answered 200 first try — QLever now REDIRECTS (qlever.cs.uni-freiburg.de → qlever.dev); always use -L on GET. 3000 rows, head Q136746190 935, tail cuts at 176. Saved tmp-sl-ranked-20260910-sc3.json in the worktree ({rows, source, ts} dict shape).
- Junk head deepest yet: ranks 0-1173 ~ALL refusals (day-of-year Q14795564 wall + colon junk; cfg hits Q122/Q126/Q125/Q123/Q121 months, Q128 Wednesday, Q2255+ day-of-year). First KEEP at rank 1174 (Q7150 ecology 228). Paintings Q12418 Mona Lisa 146 / Q45585 Starry Night 76 fetched first (residue-carry) but ranked BELOW the 223-band walk picks → did NOT make top-10 (10 walk picks outranked them).
- Walk ranks 1174-1319 → 12 KEEP in ~60 rows, band 228-223; refusals ~95 ref-p31 (Q14795564 wall), 0 ref-colon, 0 fetchfail, 0 cfg/PR overlap. PR sweep: 1 open PR (#905 class-scout strait batch, 10 QIDs, zero overlap).
- Mid-run main moved TWICE (158daf7f → 07fd7953 → ab64c400; cfg QIDs 1364→1374). Pinned-sha refresh pattern held: re-dump cfg + PR sweep + pick overlap zero, then land pinned at ab64c400. Config grew to 2618 maps / 1343 EntityData URLs at land time.
- TAIL SHAPE: blank-tolerant bare-] CONFIRMED STILL CURRENT at ab64c400 (`...true}` / `]` / BLANK / `}`), but the shape-A probe MUST scan back over blank lines from the final `}` to the bare `]` — a naive lines[-2] check sees the blank and dies rc7. Insert batch BEFORE the `]` line; keep blanks untouched.
- PROPOSAL-TEMPLATE EXTRA-BRACE BUG (run-z trap, new variant): with the last map self-closing `...true}`, tail `...true}]}` needs NO trailing `}` — the old `%s%s}` template emitted braces 11 vs closes 12. STRUCT brace-count assert caught it pre-gate. Correct: tail = '...true}]' + '}' fused = `true}]}` with the trailing `}` only as part of close_v. ALSO: the tail assert must expect `true}]}` (map close + `]` + `}`), not `true` + `]}` — a `endswith('true'+']}')` check false-fires on the correct fused tail.
- Gate 10/10 exit 0 with byte sizes == own fetches (crosschecked 10/10 against verify_*.json + ownfetch_*). Evidence base 2613 → branch 2623 exit 0. per-id evidence greps 2x each. ls-remote==HEAD. PR #914 OPEN MERGEABLE/CLEAN at create + re-poll; body read-back 1679 chars (all 10 QIDs + GATE_EXIT=0 + 2623/2613, no stale-band tokens).
- Landed #914: branch bot/sitelink-scout-20260908-2250, commit 21827589 (config-only +62/-0, maps 2618→2628, urls 1343→1353), base ab64c400.
- Picks: Q7150 ecology 228, Q9128 light 228, Q1399 Niccolò Machiavelli 227, Q3594268 Šiprage 224, Q18498 grey wolf 224, Q7204 Middle East 224, Q6583 Sahara 224, Q1124 Bill Clinton 223, Q1612 Arkansas 223, Q1522 New Mexico 223.
- Next run: paintings Q12418 Mona Lisa 146, Q45585 Starry Night 76 STILL UNCLAIMED (residue-carry, verify live). Then walk tmp-sl-ranked-20260910-sc3.json from rank 1320 (row 1319 = Q49625 2022, 223; rows 1320+ re-derive from EntityData). Ranking fresh this run; discovery query due again in ~2-3 runs (tail cuts at 176).

## 2026-09-08 run gg (PR #875) addenda

- UNTRACKED-TRACKED COLLISION blocks detach: 82 untracked src/*.cljc scratch files from old runs are now TRACKED on origin/main, so `git checkout --detach` refuses ("untracked working tree files would be overwritten") — a third refusal variant besides phantom-dirty and stale-branch. Fix: parse the blocked list from the error, assert each path is NOT tracked at HEAD (`git ls-files --error-unmatch` rc!=0), rename to /tmp quarantine, detach. Never move tracked files.
- NEW TAIL SHAPE: bare-] BLANK-TOLERANT — config tail is `   :access "public" :interval-seconds 86400 :llm? true}` / `]` / BLANK / `}`. Splice v3 scans back from the final `}` over BLANK lines to the bare `]` and inserts the batch before it (no close re-fusion at all). Measured line_delta 60 = 60 batch lines + 1 comment; numstat +61/−0. Land v3 now enumerates 4 shapes incl. blank-tolerant bare-] and dies on anything else.
- TEMP-WORKTREE nbb cwd TRAP: `nbb --classpath src ... --root .` runs against the CALLER's cwd, not the worktree path — launching it from the sitelink worktree with the temp worktree only listed in args re-measured MY branch (2491) when I wanted main (2481). Run nbb with cwd=<temp worktree> (subprocess cwd param), then worktree remove.
- Mid-run sibling merge at the SAME tail (commons-scout #872, 10 commons maps, zero EntityData overlap): keep-both merge via difflib added-blocks — both sides' inserts sit at the IDENTICAL base position (16174), no closes inside either added set, resolution = base + mine + theirs at that position. Multiset id assert: (branch − base) == mine(10) ∪ theirs-new(10), (base − branch) == ∅. Merge commit 70f3d1ff, both parents verified.
- Recoverable-abort chain that held: rc7 splice-shape abort (pre-write, harmless) → rc3-style assert on line_delta 60 vs hardcoded 61 (fix sed, retry) → evidence-filename mismatch crash AFTER commit+push (evbase wrapper wrote evbase.log, land expected evidence_base.log) → recover_pr verified branch/parent/ls-remote/id-delta and created PR. Commit-then-crash is always recoverable; never re-splice a committed branch.
- Gate-format lesson recurred: SOURCE lines are `SOURCE\twikidata-qNNN\tHTTP 200\tNNNNB\tok` — parse field 3 and strip the trailing B; proposal tail must FUSE the last map's close with the vector+top close (`...true}]}` via chr(93)/chr(125), asserting `true` immediately before).
- Landed #875: base 313ec326 (run-ff pin held through landing), branch bot/sitelink-scout-20260908-1714, batch ffbb4165 (+61/−0), merge 70f3d1ff, gate 10/10 exit 0 (byte sizes == own fetches), evidence main 2481 → branch 2491 (+10 exact) exit 0, per-id greps 2x each, ls-remote==HEAD both pushes, PR OPEN MERGEABLE, body read-back verified.
- Residue (EntityData-verified this run, unclaimed): Q3594268 Šiprage 224, Q18498 grey wolf 224. Element lane continues below the newly-configured band — unclaimed (Discovery-count references from run-i, RE-MEASURE from EntityData before use): Q623 carbon 218, Q663 aluminium 210, Q560 helium 208, Q627 nitrogen 205, Q568 lithium 194, Q682 sulfur 194; paintings Q12418 Mona Lisa 146, Q45585 Starry Night 76. Next run: residue-first, then walk tmp-sl-ranked-20260910.json from rank 1295; fresh QLever discovery query DUE (17th adoption of the 09-10 ranking; tail cuts at 176).

## 2026-09-08 run ff (PR #868) addenda

- PRE-RUN REFUSED + STALE-BRANCH COMBO: the refusal found the worktree sitting on leftover sibling branch `pq-tmp-861` (not detached), config NOT actually dirty (phantom-dirty pattern AGAIN — tracked porcelain + `git diff HEAD` empty). Diagnose with `git status -b`, then status-refresh + detach retry succeeded.
- RANKING LOADER FIXED: run-1315's 4-shape loader gap closed — bindings-dict path (`raw["results"]["bindings"]`, item.rsplit('/'), links int) parsed the 20260910 ranking natively (3000 rows, head Q136746190 935 anchor-asserted). 16th ranking adoption, no discovery query burned.
- Residue-first 3/3 exactly as run-1315 predicted: Q1415 Indiana / Q1412 Finnish / Q1780 Bratislava, all 225, unclaimed, P31 clean (Q35657 / Q1288568+Q33742 / Q515 city). Walk ranks 1225-1258 → 12 KEEP in 34 rows, band 226-225; refusals 8 ref-p31 (day-of-year Q14795564 wall), 6 ref-colon, 8 cfg (exactly #861's own batch). 43s walk.
- Top-10 by (links desc, rank asc): Q2844 Brasília 226, Q1166 Michigan, Q14773 Macau, Q1558 Kansas, Q1773 Riga, Q1780, Q1415, Q16867 Edgar Allan Poe (P31 Q5), Q1537 Wisconsin, Q1412. Residue: Q49628 (2021, P31 Q3186692) 225, Q219060 Palestine 225.
- Mid-run main move b740a113→e1ca1a81 (#866 commons, EntityData +10 = 1273 urls) fired pinned-sha rc3 PRE-WRITE (land_state.json never written — recovery scripts must rebuild picks from keeps/decisions, not depend on land artifacts). Refresh: cfg re-dump, PR sweep (0 open), pick overlap zero → no re-walk.
- TAIL SHAPE ROTATED AGAIN between base and land: pre-move tail was fused-`true}]`+`}` (#863 zoo re-fused the close onto ITS last map), post-move tail is standalone `]` then `}`. Land v2 enumerates THREE shapes in-script (standalone-] / bare-]-fused / fused-]}) and dies on anything else; measured deltas +10/+10/+10/+10; structural string/comment-aware scan OK; numstat +60/−0 (fused-line context effect).
- Landed #868: base e1ca1a81, branch bot/sitelink-scout-20260908-1530, commit 7c06e8b9 (parent==pin, config-only), gate 10/10 exit 0 twice with byte sizes == own fetches (182538/324640/359981/253568/298531/221180/237568/373497/228293/155210), base evidence 2439 → branch 2449 (+10 exact) exit 0, per-id greps 2x each, ls-remote==HEAD, PR #868 OPEN MERGEABLE/CLEAN, body 1888 chars read-back exact, post-create ancestor rc=0, main quiet through finish.
- Next run: residue-first Q49628 (2021) 225 + Q219060 (Palestine) 225 (re-verify live), then walk tmp-sl-ranked-20260910.json from rank 1259. Discovery query DUE within ~1-2 runs (ranking fresh 09-10, tail cuts at 176). PR #865 class-scout claims 10 wikidata QIDs — class lanes never overlap the sitelink band but always re-sweep anyway.

## 2026-09-08 run ee (PR #836) addenda

- FUSED-TAIL VARIANT #2 on this base (7ddfc43a→cd38384c): the whole close is on the LAST MAP's line — `...:llm? true}]}` with NO standalone close line, PLUS a trailing `''` from split. Geometry handling that passed: pop trailing empties; if last.rstrip().endswith(chr(93)+chr(125)) and ':interval-seconds 86400' in it → FUSED: stem = last_line minus the two close glyphs, insert batch after stem, re-fuse batch[-1] + close, append ''. Asserts: delta +10/+10, each new id x1 (exact-token {:id form), each QID URL token x1, no uppercase-residue ids, zero standalone closes outside EOF.
- Residue-first 6/6: run-dd residue (Q3711 Belgrade/Q641 Venice/Q11639 dance/Q1391 Maryland/Q1312 spring/Q1156 Mumbai, all 229-230) re-verified live and unclaimed. Walk ranks 1136-1158 → 6 KEEP (Q216 Vilnius/Q7362 ear/Q771 Massachusetts/Q7191 Nobel Prize/Q753 copper/Q7860 nature, all 229) in 23 rows; 10 refusals = 7 ref-p31 (Q14795564 day-of-year wall continues) + 3 ref-colon; 0 fetchfail; ~30s total.
- STAGED-STATUS assert fired a third time (land script aborted AFTER splice+branch-create, commit intact): assert must normalize BOTH codes — `st.replace(' ','').strip()=='Mconfig/knowledge-ingest.edn'` (unstaged ' M ...' vs staged 'M  ...'). Recovery: re-verify on-disk geometry, commit from the already-created branch, parent==base, +72/1- diff, then evidence.
- PR-CREATE-TUPLE BUG (new, cost 3 no-op retries): `rc, anc, _ = sh(['git','merge-base','--is-ancestor',...])` then `'%d' % anc` — but sh() returns (rc, out, err), so anc is the STDOUT STRING ('') not the code → TypeError exit 1 BEFORE any gh call. Dead-stdout hid the traceback each retry. Every sh() call site must unpack 3 values or use r.returncode directly; the fix script also logs err on create failure. gh pr create then answered 200 first-try: PR #836, headRef read-back == pushed sha, body checks all pass (10 ids, GATE_EXIT=0, REJECTED 0, 2259/2269, no stale band tokens).
- mergeable UNKNOWN→MERGEABLE/CLEAN verified <15s after create; poll loop must write its log INSIDE the loop (a loop that only writes after break loses all lines if killed).
- Residue (EntityData-verified this run, re-verify before use): Q753 copper 229, Q7860 nature 229. Next run: residue-first, then walk tmp-sl-ranked-20260907b.json from rank 1159 (next rows Q663/Q5647 band ~228, ref-p31 Q14795564 dense). Ranking tail cuts at 176 — fresh QLever discovery query (minimal no-FILTER shape, explicit PREFIX wikibase) is DUE now as the 229→176 band drains.
- Evidence this run: branch 2269 = base temp-worktree 2259 + 10, exit 0 both, per-id greps 2x each; commit 1a3aeeb5 parent==base cd38384c; ls-remote==HEAD; body via run-unique /tmp/sl9/sl9_pr_body_ee.md (2479 chars) read back exact.


## 2026-09-08 run dd (PR #830) addenda

- TAIL-SHAPE ROTATION CONFIRMED: run-cc's fused `... :llm? true}]}` EOF is gone; #828's steam-games-7 batch now closes the vector (steam map closes `377160]}` + standalone `]}` at EOF). The 'prev line is a map close' assert must accept ANY `}`-ending map close (steam appid `]}` or wikidata `true}`), never a hardcoded shape — enumerate the tail every run, refuse on surprise, never assume.
- `:source-classes [...]` vectors count as bracket pairs: STRUCT check on the proposal must expect len(sources)+1 brackets (11 for 10 picks), not 1 — a `==1` assert false-fires on a healthy proposal and cost one aborted step3 run (zero side effects, asserts-first held).
- Double-escaped `\n` in a python `-w '\n%{http_code}'` curl arg makes the BODY the 'http_code' (separator never appears) → endless 45s retry loops at HTTP 200 with 0 accepted fetches; one 420s timeout burned. Fix: `-o file` for the body + bare `-w %{http_code}` for the code; never parse a separator. Symptom 'every residue 200-bytes-0' = this bug, not the network.
- The first 420s terminal timeout left the python process ALIVE appending to the same log; killed by exact script-name ps match before the retry (kill wrapper AND python child, verify 0 alive).
- Run: no discovery query (15th adoption of 20260907b ranking); PR sweep 3 open PRs → 0 wikidata claims, re-swept at land → 0 open. Residue-first 4/4 (Q7561 snow 231, Q1311 winter 230, Q1261 Colorado 230, Q1055 Hamburg 230) + walk ranks 1114-1135 → 12 KEEP in 22 rows (Q10693 periodic table/Q1075 color/Q1770 Tallinn/Q23442 island/Q26 Northern Ireland/Q282 wine 230, Q3711 Belgrade/Q641 Venice/Q11639 dance/Q1391 Maryland/Q1312 spring/Q1156 Mumbai 230-229); 10 refusals (3 colon-junk, 7 day-of-year Q14795564). Top-10 by measured count; band 231-230.
- Landed #830: base 115378d9 (main moved twice pre-gate 412ecb97→9c0f1a1e→115378d9, absorbed by refresh; then QUIET through finish), commit c2246cc1 config-only +60, maps 2230→2240, urls 1143→1153, gate 10/10 exit 0 byte-sizes == own fetches, evidence 2235 (= base 2225 + 10) exit 0, per-id greps 2x each, ls-remote==HEAD, PR OPEN MERGEABLE/CLEAN, body read-back 2549B all 10 QIDs.
- Residue (EntityData-verified this run, re-verify before use): Q3711 Belgrade 230, Q641 Venice 230, Q11639 dance 229, Q1391 Maryland 229, Q1312 spring 229, Q1156 Mumbai 229. Next run: residue-first, then walk tmp-sl-ranked-20260907b.json from rank 1136 (sl_last_rank.txt). Ranking tail cuts at 176 — fresh QLever discovery query (minimal no-FILTER shape, explicit PREFIX wikibase) genuinely DUE within ~1-2 runs as the 230→176 band drains.

## 2026-09-08 run cc (PR #825) addenda

- NEW TAIL SHAPE in the sitelink worktree: the vector+top close is FUSED with the last map's own close on ONE line — `   :access "public" :interval-seconds 86400 :llm? true}]}` at EOF, no standalone `]}` line. Run-hh's 'prev line is a map close' assert aborts on it. Geometry handling that passed: if last.rstrip() ends with `]}` (chr(93)+chr(125)) AND contains `:interval-seconds 86400` → fused shape; strip the two close glyphs from the last line, insert batch lines (last batch map closes `...true}`), then RE-FUSE `batch_last_line + ]}`. If a standalone `]}` line exists instead → old shape. Never assume either.
- run-m CASE BUG RECURRED (third strike): the batch generator built `"wikidata-" + info["qid"]` where qid already carries the Q → ids landed as `wikidata-Q5816` (uppercase). Evidence did NOT flag it (sources-configured 2201 = +10 exact, exit 0) — only the per-id verification greps caught it. Rule sharpened: the walker stores qids WITH the Q; the generator must use `"wikidata-" + qid.lower()`. Detection: per-id greps must grep the LOWERCASE form and assert 0 uppercase-residue mentions; expect exactly 2 hits each (configured + idle sections).
- Case-bug repair without force-push: follow-up commit on the same branch (exact-token replace `{:id "wikidata-Qn"` → `{:id "wikidata-qn"`, assert each 1→0 / 0→1, map+url counts unchanged), evidence re-run (still 2201 exit 0), push fast-forward, `gh pr edit` to append the fixup note, re-read mergeable CLEAN. Full cycle ~2 min.
- Walk: residue-first Q5816 Mao Zedong 232 / Q6527 Rousseau 232 (re-measured live) + ranks 1090-1113 → 12 KEEP in 24 rows, band 232-230; 8 ref-colon, 4 ref-p31 (day-of-year Q14795564), 0 fetchfail, 0 cfg hits, 0 PR claims (0 open PRs). Picks: Q5816/Q6527 232, Q1428 Georgia / Q1227 Nevada / Q1408 New Jersey / Q19837 Steve Jobs / Q1930 Ottawa / Q1877 Đồng Hới / Q44 beer / Q7377 mammal 231. US-state P31 Q35657 rows ride the band cleanly (first in series).
- Residue (EntityData-verified, unclaimed): Q7561 snow 231, Q1311 winter 230, Q1261 Colorado 230, Q1055 Hamburg 230. Next run: residue-first, then walk tmp-sl-ranked-20260907b.json from rank 1114 (write sl_last_rank.txt). Ranking tail cuts at 176 — fresh QLever discovery query due within ~1-2 runs.
- Landed #825: base 52a83292, commit f5cfa5e6 (config-only +61) + fixup a4f3227c (id case, 10+/10−), gate 10/10 exit 0 with byte sizes == own curl fetches, evidence 2191→2201 both exit 0, per-id greps 2x each lowercase, ls-remote==HEAD after both pushes, PR OPEN MERGEABLE/CLEAN, body read-back verified (1865 chars, all 10 QIDs + GATE_EXIT=0 + 2191/2201).

## 2026-09-08 run jj (PR #820) addenda

- NEW SILENT-NOOP TRAP: invoking a PYTHON script via `zsh script.py` (not `python3`) exits 0 and runs NOTHING - the refresh step produced no log and no side effects; ~8 min lost before noticed. read_file dedup then masked the crash ('File unchanged') because the script never wrote its log; the decisive signal was reading the WRAPPER's own log (traceback/empty) instead of re-reading the script log.
- Residue-first delivered 6/10: all run-ii residue (Q4421 forest, Q5806 Mecca, Q760 Saint Lucia, Q7569 child, Q7368 sheep, Q629 oxygen) re-measured 234 live and unclaimed. Walk ranks 1022-1089 (sl_last_rank=1090): the 235/234 band is a day-of-year Q14795564 wall (17 ref-p31) plus colon junk and cfg hits Q8072 volcano/Q709/Q1400/Q1397/Q1370/Q25290 (2017, already configured). Walk KEEPs: Q8445 marriage 234, Q34424 Shakira 233 (en label None; title built from the en DESCRIPTION 'Colombian singer-songwriter (born 1977)' - descriptions are the fallback for ja-only items), Q8074 cloud 233, Q2280 Minsk 232. Pool 12, top-10 by measured count.
- Mid-run main move between gate attempts (60821525->3be1c8bf, #819 gp-source, cfg EntityData count UNCHANGED 1103): rc3 abort pre-write; refresh2 (re-detach + cfg re-dump + PR sweep 0 + pick-overlap -> all zero) + re-gate on the new base with identical byte sizes. No walker rerun.
- Gate byte-size cross-check: my regex didn't match the gate's tab-separated SOURCE lines (`SOURCE<TAB>wikidata-qNNN<TAB>HTTP 200<TAB>NB<TAB>ok`); compare the NB values against ownfetch directly (all 10 matched exactly).
- Land v2: 'prev line is a map close' assert must be `':interval-seconds 86400' in prev and prev.endswith('}')` - #819's last maps are `:llm? false}` (v1's endswith(':interval-seconds 86400') fired harmlessly pre-write). Splice +62/-0 (maps 2162->2172, urls 1103->1113), commit 8c6de08d parent==base, evidence 2167 exit 0, per-id greps 2x each, ls-remote==HEAD, PR #820 OPEN MERGEABLE/CLEAN at poll 0, body read-back 1275B with all 10 ids + GATE_EXIT=0 + 2167.
- Next run: residue-first Q5816 Mao Zedong 232, Q6527 Jean-Jacques Rousseau 232 (EntityData-verified this run, re-verify before use), then walk tmp-sl-ranked-20260907b.json from rank 1090 (next rows: Category:/day-of-year heavy). Ranking tail cuts at 176 links - fresh QLever discovery query (minimal no-FILTER shape) is DUE within ~1-2 runs as the 231->176 band drains.

## 2026-09-08 run ii (PR #816) addenda

- UNTRACKED SCRATCH NOW SURVIVES IN THE WORKTREE: tmp-sl-ranked-20260907b.json lived in the sitelink WORKTREE (not /tmp) with a NEW SHAPE — `{"rows": [[qid, links]...], "source": ...}` dict; parse `raw["rows"]`, not the file itself. Order anchor before reuse: ranks 1020/1021 must be Q5649364/Q7944 (run-hh's last decisions). 14th adoption; still no discovery query burned (tail cuts at 176, resume run-jj at rank 1062).
- .KOTOBA OVERLAY GOTCHA IS OBSOLETE (as of base de447882): the gate ran clean with ZERO *.cljc.kotoba overlay files present and `scripts/generate_kotoba_overlays.cljs` does not exist — don't hunt for an overlay generator; run the gate directly. node_modules/ symlink already exists in the sitelink worktree; the temp-worktree base-evidence pattern still needs it linked in.
- Evidence at 2141 sources finishes standalone in well under a minute (foreground OK); base-evidence via `git worktree add --detach /tmp/sl_ii_base_wt <sha>` + node_modules symlink gave base 2131 vs branch 2141, exact +10, both exit 0.
- Mid-run main move #813 (99e47ce7→de447882, +10 gp-source maps, zero EntityData overlap) landed BETWEEN gate and branch-create: the one-shot land script's fetch+reset re-pin + parent==base assert handled it without touching the walk or the gate (gate byte sizes unchanged).
- Walk ranks 1022-1061: 12 KEEP in 40 rows (11 ref-colon, 17 ref-p31 ALL day-of-year Q14795564 — the refuse-set entry carried the whole calendar band again), 0 fetchfail, 0 PR claims (0 open PRs, verified via raw API after a `gh pr list` 0-result). Residue-first (Q3947/Q25274/Q2316/Q7944, all re-measured 236) outranked walk rows at 236-234; pool assembled 16, top-10 by measured count.
- PR-body read-back staleness check: list expected-ABSENT tokens from the PREVIOUS run (old residue QIDs, old branch name) — but check the list against what the body legitimately cites; this run 'run-hh' appears BY DESIGN (anchor verification note). Sanity checks must know the body's own vocabulary or they false-positive.
- Landed #816: branch bot/sitelink-scout-20260908-0325, commit d062655e (config-only +61/−0, parent == de447882), maps 2136→2146, EntityData urls 1093→1103, gate 10/10 exit 0 (byte sizes == own fetches), evidence 2131→2141 exit 0, per-id evidence greps 2x each, ls-remote==HEAD, PR OPEN MERGEABLE/CLEAN, body-file 2014 chars verified post-create.
- Next run: residue-first Q4421 forest 234, Q5806 Mecca 234, Q760 Saint Lucia 234, Q7569 child 234, Q7368 sheep 234, Q629 oxygen 234 (EntityData-verified this run, re-verify before use), then walk tmp-sl-ranked-20260907b.json (worktree, {rows:...} shape) from rank 1062. Fresh QLever discovery query (minimal no-FILTER shape) due within ~1-2 runs as the 234→176 band drains.

## 2026-09-08 run hh (PR #811) addenda

- LAND-SCRIPT STATUS ASSERT MUST FILTER UNTRACKED: `git status --porcelain` (no flag) returns the worktree's 150+ untracked scratch files alongside the staged config, so a `len(stl) != 1` assert aborted AFTER a fully-verified splice (asserts-before-write held; nothing lost). Correct assert: `--untracked-files=no`, and `st.replace(' ','')=='Mconfig/knowledge-ingest.edn'` (run-t's strip rule). Recovery = re-verify on-disk geometry (maps 2112→2122, urls 1073→1083, per-id tokens x1, single close at EOF, batch comment x1) then commit — sl_recover.py pattern; keep splice constants and recovery constants identical.
- NEAR-EMPTY PR SWEEP VERIFIED BY RAW PATCH DUMP: 3 open PRs (806 gp-source, 807 NSF slices, 808 commons) showed 0 QID tokens in `gh pr diff`; dumped all three patches in full via `gh api pulls/N -H 'Accept: application/vnd.github.diff'` (small diffs — just read them) and confirmed 0 wikidata EntityData claims. All three merged mid-run as ONE commit e7c11df9 (+14 non-wikidata maps, EntityData URL count unchanged 1073) — pinned-sha rc3 abort fired between gate and land exactly as designed; refresh = re-dump cfg (cfgq 1104, overlap []) + re-gate on e7c11df9 (identical 10 byte sizes) + re-pin BASE via a tiny sl_setbase.py regex rewrite (patch tool still unreliable on fresh files).
- Walk: residue-first Q3870/Q7386 + ranks 996-1021 of 20260907b ranking → 12 walked KEEP in 26 rows, band 237-236, 14 refusals (7 colon-junk, 7 day-of-year Q14795564), 0 fetchfail. Top-10 by count took both residue + Q62 San Francisco/Q757 StVincent/Q19939 tiger/Q19660 Bucharest/Q25291 2018/Q450675 Pope Francis/Q25245 2016/Q2102 snake. Year entities Q25291/Q25245 (P31 Q3186692) rode the band per #787 precedent.
- Landed #811: branch bot/sitelink-scout-20260908-0252, commit f2edbc44 (config-only +61/−0, parent == e7c11df9), evidence base 2107 (temp worktree) → branch 2117 exit 0, per-id greps 2x each, ls-remote==HEAD, PR OPEN MERGEABLE, body read-back (1246B, 10 ids, GATE_EXIT=0), ancestor check 0.
- Next run: residue-first Q3947 house 236, Q25274 2019 236, Q2316 Kurów 236, Q7944 earthquake 236 (EntityData-verified this run, re-verify before use), then walk tmp-sl-ranked-20260907b.json from rank 1022 (sl_last_rank.txt). ~1980 unused ranking rows remain above the 176-link cut — fresh QLever discovery query due within ~2-3 runs as the 236 band drains.
- Mid-run main moves ×2 handled by the pinned-sha rc3 abort (84b8ad19 → fe7d49ad #801/#802 BEFORE gate; c0cbec76 #803 AFTER PR create). #801/#802 added zero wikidata EntityData URLs (cfg 1084 QIDs unchanged); #803 (feed-scout ioc) didn't touch the config at all — merge-tree --write-tree clean, mergeable MERGEABLE/CLEAN at poll 0. No manual merge needed; body read-back verified (1837 bytes: GATE_EXIT=0, 2073/2083, all 10 ids, residue QIDs absent).
- Landed #805: branch bot/sitelink-scout-20260908-0155, commit 57befb19 (config-only +61/−0, maps 2078→2088, EntityData urls 1053→1063), gate 10/10 exit 0 on fe7d49ad with byte sizes == own curl fetches, evidence exit 0 both trees, ls-remote==HEAD, PR OPEN MERGEABLE/CLEAN.

## 2026-09-07 run aa (PR #787) addenda

- ZSH SUBSCRIPT TRAP (new, cost a partial land script): inside a double-quoted zsh string, `$q[.]json` parses `[.]` as an ARRAY SUBSCRIPT — every loop iteration died with "bad floating point constant" and the token check printed empty. Historical bash `$q[.]`-style grep patterns are unsafe in zsh -c wrappers. Fix: per-id token checks belong in the python script (which already asserts them), never in shell loops.
- .KOTOBA OVERLAY GOTCHA #2: the overlay .cljc copies MUST exist before the gate runs, but the overlays are only needed for the GATE (verify_source_proposal requires hyakka.corpus.registry). wiki_growth_evidence.cljs ran FINE plain (`--classpath src`) because it does not require the registry — confirmed twice this run (branch evidence and temp-worktree base evidence). Also note `find src -name '*.kotoba' | wc -l` = 82 but corpus/ holds only 30 of them; the overlay generator must walk all of src/.
- WALKER TEMPLATE (scripts/sl_walk.py, reusable): sequential urllib fetches with UA header; colon-fraction + label-prefix(en+ja) + mainpage + P31-refuse-set triage; renames prior sl_decisions.jsonl/sl_picks.json to *.bak at startup; adoption loop re-decides undecided rows and copies decided ones into the new JSONL in rank order; MAX_KEEP=12 leaves residue; prints STATS + PICK lines into sl_walk.log. This run: 938 rows → 12 KEEP (Q2021 247, Q1994 247, Q685 244, Q166/Q3640/Q2234/Q5292/Q2228/Q239/Q4/Q905 240, Q89 240), 419 ref-colon, 6 ref-p31, 493 cfg, 9 hard, 0 fetchfail, in ~350s sequential.
- RESIDUE-NOTE LABEL BUG hit AGAIN (run-ee standing rule): run-cc's residue note said "Q2021 (2006) / Q1994 (2011)" and the EntityData labels this run CONFIRM those two, but the accompanying labels for other QIDs were garbage as always. Only the QID+count pairs from notes are usable; re-measure labels live every time.
- P31-refuse set additions that fired correctly: month-of-year Q47018901 rows (Q122 Q126 Q125 Q123 Q121 all refused), day-of-week Q41825 (Q128 Wednesday refused), Q13406463 list-article (Q11750 kept earlier by run-ll's decision; this run did not re-test it).
- WDQS 504'd twice again (full scan); QLever top-3000 answered first-try with the explicit `PREFIX wikibase:` query — TWO new failure shapes before that: (a) curl -G with a 24-byte response was a WDQS 504 body, (b) the first QLever file was accidentally written WITHOUT the PREFIX line (99 bytes) and returned "Prefix wikibase was not registered" — check the query file CONTENT, not just the endpoint, when QLever 400s.
- GIT MERGE-CONFLICT RECIPE #3 (one-hunk, HEAD-side-is-just-the-close): after a sibling merges a tail batch, `git merge origin/main` can produce a hunk where HEAD side = `]}` alone and THEIRS = their maps + close. Resolution = THEIRS content (minus close) + one close; do NOT try to find "your" maps in the HEAD side (they are outside the hunk, already common). Distinguish from the both-sides-self-close case (#785 hunk: HEAD = my batch + close, THEIRS = lake batch + close) by CHECKING whether your batch ids are in the HEAD side before picking a recipe — never assume.
- MERGE-SCRIPT RESTART HAZARD: a backgrounded merge script that dies AFTER `git merge` leaves `UU` state; re-running a naive script fails with "Merging is not possible because you have unmerged files" (rc 128). The resolver must be IDEMPOTENT: if status is UU, skip the merge and go straight to conflict resolution; only run `git merge` on a clean tree.
- gh pr edit + body read-back worked (run-n pattern, run-unique body file); mergeable flips UNKNOWN→MERGEABLE in <15s on this repo currently; two full CONFLICTING→MERGE cycles completed inside one run.
- Evidence arithmetic this run (base-relative): base 3de09f30 = 1961 → branch +10 = 1971; after merge of #785 (main 1971) merged branch = 1981; after #786 (main 1973) final merged = 1983 = 1973 + 10 mine. All asserted dynamically, never remembered.
- Reusable scripts committed nowhere — they live in the worktree scripts/ (sl_walk.py, sl_splice.py, sl_commit.py, sl_refresh_check.py, sl_resolve.py, sl_merge2b.py, sl_check.py) and are untracked scratch; copy patterns from them, not the files.
- Landed #787: branch bot/sitelink-scout-20260907-2148, batch commit 5b2141bc (config-only 62+/1−, maps 1976→... urls 993→1003), merges 8efce127 + e964abd7; gate 10/10 exit 0 twice (93fdf10f and 3de09f30 bases, identical byte sizes); evidence 1961→1971→1981→1983 all exit 0; per-id evidence greps done via python token counts (zsh trap avoided); ls-remote==HEAD after every push; PR #787 MERGED as e964abd7 within the run window (state read-back: MERGED, headRefOid == ls-remote == e964abd7); body 4696 chars verified post-edit with all tokens present. Substring evidence greps (grep -c 'wikidata-q4') are inflated by q4xxx siblings — exact-token {:id counts are the authoritative check (all 10 verified x1 at splice, commit, and both merge stages).
- Next run: walk tmp-sl-ranked-20260907.json from rank 939 (rows 939-3000 unused; the walk stopped at MAXKEEP 12 with rank 938 = Q905 Kafka KEEP). Residue (EntityData-verified this run, re-verify before use): Q89 apple 240, Q905 Franz Kafka 240. Junk head this run ended around rank 400; the 247-240 band sat at ranks ~920-938. Ranking tail cuts at 176 links — fresh QLever discovery query due within ~2-3 runs.


## 2026-09-08 run bb (PR #797) addenda

- Calendar-unit POLICY generalization: day-of-year Q14795564 entities (Feb 1, Jan 13, ... ~12 of them) ride the whole 239-band exactly like month-of-year Q47018901 did — they swept the top of the fresh ranking's encyclopedic tier (ranks 939-967 were ~half day-of-year rows). Refuse them (added Q14795564 to the P31 refuse set) and hard-exclude Q5151 'month'; the walker dropped 5 KEEP-stage picks + adoption re-dropped them, and 7 more were refused below. Year entities (Q1999 '2014', precedent #787's Q2021/Q1994) left as residue only.
- Tail-assert bug that wasted two gate runs: the fused-close assert expected ':llm? true}]' as the body suffix while the generator correctly emitted '...true}]}' (map close + vector close + top close = ]+}+}). Fix the ASSERT (build with chr(93)/chr(125), never hand-type the two-glyph token), not the generator. The braces/brackets balance STRUCT checks pass on a balanced file, so they do NOT catch this — the tail assert is the one that fires.
- OWN-URL byte-check discipline: to compare gate byte sizes against my own fetches, extract the EXACT :url strings from the proposal file and re-fetch those. A 'Q'+qid.upper() derivation typo'd 'QQ89' and drew 400s — looked like a gate mismatch but was the checker's own bug. Correct derivation: q = nid[len('wikidata-'):].upper(). After the fix the re-fetch matched gate byte sizes 10/10 byte-for-byte.
- Third mid-run main move in one run (a2a918de → 53ce42f7 → a68bfd27 → e7e56a26, then f483d37a after PR create): pinned-sha rc3 abort + refresh(cfg+PR sweep+pick overlap, no walker rerun since zero collisions) + re-gate with identical byte sizes held. But the land script's rc3 abort fired AFTER the commit (parent check) — commit sat on the OLD base. Recovery: write the committed config content onto a detached checkout of the new base, `git branch -f <BR> <new-base>`, checkout branch, re-commit (recover2.py pattern); verify commit content (ids x1, config-only diff) BEFORE re-parenting.
- CONFLICTING flip after PR create (main absorbed #793 + gp-relation): resolution = main's config wholesale + my batch lines (extracted from MY commit between the batch comment and its close) re-appended before the single close at EOF; multiset id check (branch−main == exactly my 10, main−branch == ∅), evidence on the merged branch (2039 = main 2029 + 10), push, mergeable MERGEABLE, body updated via gh pr edit + read-back. Merge script must be IDEMPOTENT: on rerun with UU state present, skip `git merge` and go straight to resolution (merge3.py); the first attempt crashed mid-resolution on the QQ89 assert and left UU state behind.
- Residue for next run (EntityData-verified this run, re-verify before use): Q5685 Anton Chekhov 238, Q1999 2014 238. Fresh ranking tmp-sl-ranked-20260907b.json (3000 rows, tail cuts at 176) has ~2040 unused rows from rank 968.

## 2026-09-08 run gg (PR #805) addenda

- PROPOSAL-TAIL REGEX TRAP (new): parsing the sources block out of the proposal with `:sources \[\n(.*?)\n \}\}` matches the LAST MAP's inner close, not the top-level fused close — the proposal's own tail is `... :llm? true}\n ]}`. Abort fired pre-write (asserts-first held); recovery = build batch lines STRUCTURED from the picks JSON (id/url/title/license/classes/access fields per map), never by parsing the proposal back. The proposal file is gate input only; the splice source of truth is picks.json.
- Landing used ONE-SHOT branch+splice+commit (run-j pattern): pre-splice geometry asserts (endswith ]}, single close, prev-line map-close, maps/urls deltas), staged-status assert, diff-vs-main asserts (+61/−0, adds == batch lines, dels == 0), parent == base. All passed first try on re-run; branch reuse when head==origin is safe.
- Evidence on THIS config size (2083) completes in well under a minute (~15-30s) — the old 'exceeds 420s' note predates the current script; background+waiter still works fine. Base-delta via temp worktree (`git worktree add --detach /tmp/sl_base_wt <sha>` + node_modules symlink) reconfirmed: base 2073, branch 2083, exact +10, both exit 0.
- Walk (ranks 829-995 of tmp-sl-ranked-20260907b.json): 12 KEEP in 167 rows — 56 ref-colon, 23 ref-p31 (ALL day-of-year Q14795564; refuse set carried the whole 239-band), 75 cfg, 1 hard, 0 fetchfail. Unclaimed band = 238-237. Picks: Q1 universe, Q5685 Chekhov, Q1999 2014 (year classes Q235729/Q3186692/Q235676 — landable per #787 precedent), Q5119 capital city, Q556 hydrogen, Q8441 man, Q763 Saint Kitts and Nevis, Q8425 society, Q23393 Lord's Prayer, Q181 Jimmy Wales. Residue (EntityData-verified): Q3870 Nairobi 237, Q7386 Formicidae/ants 237.
- NO discovery query burned: the 20260907b ranking survived again (13th adoption); rank 829 was the resume point from run-cc's last_rank 828. Tail cuts at 176 — next run resumes at rank 996 (Q3870/Q7386 residue first); a fresh QLever query is due within ~2-3 runs as the 237→176 band drains.

## 2026-09-10 run 1300: PR #854 MERGED IN-RUN; bare-']' vector close is a NEW tail shape

- PR #854 (branch bot/sitelink-scout-20260910-1300, commit a11f9c47, merge 6c2a1ada) was MERGED within the run window — the post-create verify loop read state=MERGED directly. Queue velocity is now minutes, not hours: always poll state BEFORE expecting MERGEABLE, and verify the merge landed on origin/main (all 10 EntityData tokens x1 on the new main config).
- NEW TAIL SHAPE #3 on main (b48b916e-era): the vector close is a BARE `]` fused onto the last map's close line (`   :interval-seconds 86400}]`) with the top-level `}` on its own line — NOT the `]}` close, NOT the fused `]}`. A splice that appends `]}` double-closes (scan-negative at the tail; the deltameasure simulation caught it pre-write). Correct splice: re-emit the consumed map-close glyph ('...86400}'), insert batch, then bare `]`, then `}`. Deltas: braces +10/+10, brackets +10/+10 for 10 maps each carrying a :source-classes vector (the fused `]` is preserved, not re-added).
- The whitespace-tolerant `.strip() != ']}'` assert is a false oracle for the bare-`]` shape: ']' fails it, but so would ' ]' silently passing a `endswith(']')` check. Assert the EXACT stripped close token per shape and enumerate the tail fresh every run (standing rule) — this run's main carried THREE successive tail shapes across #849/#853 within one run.
- DELTA-MEASURE-BEFORE-ASSERT worked: instead of hand-deriving brace/bracket deltas (3 aborted splice attempts: brace-close +11 vs +10, bracket +10 vs +11), run the exact splice in memory against the base config and PIN the asserts to the measured deltas, then assert the structural scan (one top-level form ending at EOF, no negative depth).
- SLICE extraction from my own commit must split on the BATCH COMMENT + next `\n]\n` (vector close line), never on `\n]}` (split-style tail) — the two closes rotate per merge; hardcoding either breaks extraction or, worse, includes a foreign close in the batch.
- Post-rebase evidence formula absorbed #853's 10 commons: base 2339 (b48b916e) -> branch 2349 (+10 mine) -> rebased 2359 (+10 #853). Assert post == pristine_of_current_base + N + merged_count; never a remembered total.
- gh pr create answered 200 first-try with --body-file after two FATAL-ABORT pre-creates (body token-count assert expected 1 per id; current body shape carries each id exactly 2x — picks table + gate block — the run-aa calibration). Pre-create aborts cost nothing; the assert ordering held.
- Walk: 12 KEEP in 41 rows at ranks 1184-1224 of tmp-sl-ranked-20260910.json (row-shape QLever bindings dict, first row Q136746190 935). Band 227-226. Residue: Q1313 summer 226, Q3646 Krasnodar 226. Ranking tail cuts at 176 links — fresh discovery query DUE next run (the 20260910 ranking is fresh this run, so next run can reuse it; the one after will need a new query).
- Note: main can carry sibling batches whose config maps are LIVE while the evidence source-count on an OLD base differs from the sum of merges — always re-measure the pristine base count from a temp worktree, never sum merge contents from memory.

## 2026-09-10 run 1315 (PR #861) addenda — bare-] splice: the consumed glyph is a whole LINE

- RANKING LOADER GAP: the walker's 3-shape loader silently FELL BACK to 20260907b because the fresh 20260910 QLever file is a bindings dict with keys head/results/meta — rows live at raw["results"]["bindings"], item = bindings[].item.value (strip .../entity/), links = bindings[].links.value. Add that 4th shape. Harmless this run (discovery only, everything EntityData-verified; band 226-225 sits right under #854's 227-226) but the walked ranks refer to 20260907b, not 20260910 — next run: fix loader, run fresh discovery query, resume from 20260907b rank 1249 or the fresh ranking's equivalent.
- BARE-] SPLICE v2 (the recipe that passed evidence): the tail map's fused line '   :access "public" :interval-seconds 86400 :llm? true}]' belongs to the PREVIOUS map (q860172) — the splice consumes its map close AND the vector close. Correct: stem = lines[:-2]; re-emit the FULL '   :access "public" :interval-seconds 86400 :llm? true}' line (map close only, no ]); insert batch (2-space maps, first batch map closes true}); fuse ']' onto the LAST batch line ('true}' -> 'true}]'); append '}'. Deltas: opens +10, closes +10, brackets +10/+10. My v1 splice re-fused only ']' and dropped the map close — q860172 left unclosed, curly depth 1, evidence exit 2 'Unmatched delimiter ]'.
- COUNT-DELTAS ARE NOT THE EDN ORACLE: v1's internally-consistent asserts (maps +10, urls +10, ids x1, closes +9) all PASSED on the broken splice — only the evidence parser caught it. Every splice must be validated by an actual EDN read (evidence script or cljs.reader) BEFORE commit; pin count asserts to the MEASURED v2 splice (+10/+10 all four), never to hand-derived numbers.
- numstat trap: the re-fused last batch line is byte-identical to the consumed fused line, so git reports +61/−0 (not +62/−1, not +60) — the fused tail line counts as context. Assert dele==0 and config-only; derive the insertion number from the measured diff, and update the PR BODY to the measured value (stale +60 shipped at create; fixed via gh pr edit + read-back).
- amend path: land abort after commit (bad numstat assert) is recoverable — verify branch/parent/tail, fix the worktree config with splice v2, run evidence, then git commit --amend (local branch only, never pushed yet), push, ls-remote==HEAD.
- Evidence wrapper pattern that worked after exec-redirect mysteriously wrote nothing: per-line explicit redirect (nbb ... > /tmp/sl10/evidence_branch.log 2>&1; echo EXIT=$? >> same log), then grep the exit line from that log. Keep a run-local /tmp/sl<run>/ manifest via an ls script — wrong-filename read-back loops cost several calls this run.
- Reemit-count assert: ':access ... true}' substring counting counts ALL batch lines too. Correct accounting for the v2 splice: -1 consumed fused + 1 re-emit + 9 standalone batch + 1 substring inside my own fused batch line = +10 net.
- Patch tool failed again on fresh /tmp content (land.py BASE pin) — file-script replace + assert is the dependable edit path (third confirmation of the standing rule).
- Landed #861: branch bot/sitelink-scout-20260910-1315, commit ec55d27e parent==01934fdf, gate 10/10 exit 0 (byte sizes == own fetches, twice on two bases), evidence 2387->2397 exit 0, per-id greps 2x each, ls-remote==HEAD, PR OPEN MERGEABLE, body 2399 chars read back + corrected. Walk: residue Q1313 consumed as pick #1; 12 KEEP in 24 rows (20260907b ranks 1225-1248); refusals 7 ref-p31 (day-of-year wall), 5 ref-colon, 0 fetchfail, 0 cfg, 0 PR claims (0 open PRs both sweeps). Residue: Q1415 Indiana 225, Q1412 Finnish 225, Q1780 Bratislava 225.

## 2026-09-10 run sc (PR #885) addenda

- FRESH DISCOVERY QUERY OBTAINED (18th ranking): WDQS curl http 000 (nginx kill); QLever top-3000 answered 200 first-try — head Q136746190 935, tail cuts at 176, saved tmp-sl-ranked-20260908-scout.json in the worktree ({rows, source, ts} dict shape). Junk head deepest yet: ranks 0-1173 were ~ALL refusals (527 ref-colon + 133 ref-p31 over the walk), first KEEP at rank 1174 (Q9128 light 228).
- Residue-first delivered 10/10: run-gg residue Q3594268 Šiprage / Q18498 grey wolf (both 224) + element lane Q623 carbon 218, Q663 aluminium 210, Q560 helium 208, Q627 nitrogen 205, Q568 lithium 194, Q682 sulfur 194 + paintings Q12418 Mona Lisa 146, Q45585 Starry Night 76 — all EntityData-verified live, zero fetch failures (429s handled by 40s backoff).
- Standalone-] tail STILL current (`]` / blank / `}`). Splice v3 with deltas measured pre-write (+10 maps/urls/braces/brackets, depth 0, close last) landed clean; numstat is +62/−1 for shape A where the −1 is a BLANK-LINE alignment artifact (git aligns the batch's leading blank against the base's blank before the top close) — assert 'minus lines are all blank' (+62) instead of +61/−0 for this shape; content deltas are the real oracle.
- Mid-run main move 83d07f93→1d21867b (fare-scout tabi-mikake, config unchanged 1334 QIDs) caught between gate attempts: refresh (cfg re-dump + PR sweep 0 + pick-overlap zero) + re-detach + re-gate with identical byte sizes — no re-walk needed.
- Landed #885: branch bot/sitelink-scout-20260908-1855, commit aad40341 (parent==1d21867b, config-only +62/−1-blank), gate 10/10 exit 0 (byte sizes == own fetches all 10), evidence base temp-worktree 2509 → branch 2519 (+10 exact) exit 0, per-id greps 2x each, ls-remote==HEAD, PR OPEN MERGEABLE/CLEAN, body read-back verified (all 10 ids + GATE_EXIT=0 + 2509/2519, run-unique body file).
- Picks: Q9128 light 228, Q7150 ecology 228, Q1399 Niccolò Machiavelli 227, Q3594268 Šiprage 224, Q18498 grey wolf 224, Q7204 Middle East 224, Q6583 Sahara 224, Q1522 New Mexico 223, Q1612 Arkansas 223, Q1124 Bill Clinton 223.
- Next run: residue-first Q1527 Minnesota 223, Q11379 energy 223, Q1581 Missouri 223, Q1223 Washington 223 (EntityData-verified this run, outrank the element lane), then element lane Q623/Q663/Q560/Q627/Q568/Q682 + paintings Q12418/Q45585, then walk tmp-sl-ranked-20260908-scout.json from rank 1312. Fresh discovery query due again in ~2-3 runs (tail cuts at 176).

## 2026-09-10 run sc2-pre (NO LAND — pre-run refusal, report-and-stop)

- Pre-run REFUSED on 'you need to resolve your current index first' during the detach — TRANSIENT, not a real conflict: minutes later the worktree had NO unmerged entries, NO MERGE_HEAD/CHERRY_PICK_HEAD, NO staged files (only a leftover REBASE_HEAD file from the winding-down sc2 landing session); a plain `git checkout -q --detach origin/main` retry SUCCEEDED first try. Before burning a run: `git status --porcelain --untracked-files=no` + `git diff --cached --name-only` + `git diff --name-only --diff-filter=U`; if all empty, just retry the detach (index-stat variant of the phantom-dirty pattern).
- The sc2 batch (commit b0d690e8, parent afcca458 → PR #900, MERGED as 0c48de45) consumed the ENTIRE predicted residue: Q1527/Q11379/Q1581/Q1223 + elements Q560/Q568/Q623/Q627/Q663/Q682 — all 10 verified x1 on the live origin/main config via exact-token grep. STILL UNCLAIMED: paintings Q12418 Mona Lisa 146, Q45585 Starry Night 76.
- Next run: worktree left clean + detached at origin/main 0c48de45 (origin absorbs #899 NSF + #900 sc2); paintings residue-first, then walk tmp-sl-ranked-20260908-scout.json from rank 1312; fresh QLever discovery query due (18th ranking, tail cuts at 176).

## 2026-09-09 run sc5 (PR #938) addenda — burst-merge morning, cycle-loop wins

- Main moved FOUR times (0803a82b→d5d1b8d3→5aa00355→4f59fb31 + more after create). Two standalone rc3 aborts cost ~4 min each; the fix was a CYCLE-LOOP script (fetch→pin→cfg re-dump→pickcheck2→re-gate→land, 6-cycle cap, land rc3→loop): landed first-try on the cycle after the last pre-land move. The gate→land window must be seconds, not minutes, when the queue is this fast.
- TEMPLATE DRIFT COST A GATE RUN: generator emitted source maps WITHOUT `:kind :web-document` — gate exit 1 on all 10 (":kind  is not dispatched"). Never rebuild the proposal shape from memory; copy the prompt's template verbatim every run.
- Proposal tail: positional asserts (bl[-2]/bl[-3]) mis-fired twice on the correct shape (last map's :access line carries the fused close, so the lines above are :source-classes/:license). Robust assert = SEMANTIC COUNTS: exactly 9 standalone `...true}` lines + 1 fused `...true}]}` line, last line fused.
- Evidence per-id grep must be LINE-ANCHORED `^<id>\t` ×2 (configured + idle). A `\t<id>\t` mid-line pattern matched 0 and crashed the land AFTER commit (recoverable: commit intact on branch, recovery script re-verified + pushed + created PR). Evidence ids sit at line START.
- `gh pr view --json` WITHOUT the `body` field returns no body → read-back verify false-fails. Request body explicitly; also don't hardcode a checker to an old log path (prfinal.py read pr_final.json while the poll loop wrote pr_poll.json — the shell poll-1 line OPEN MERGEABLE was the truth).
- sc3 ranks 1416/1417 == the predicted residue EXACTLY (Q10987 honey 220, Q11059 Sanskrit 220); residue-first walking keeps paying. Walk 1416-1473 → 12 KEEP in 58 rows (43 ref-p31 day-of-year Q14795564 wall at the 220 band, 3 ref-colon, 0 fetchfail, 0 cfg/PR overlap). Picks: Q1494 Mississippi/Q1393 Delaware/Q1553 Nebraska/Q5838 Kabul/Q199 1/Q11460 clothing/Q16551 Vermont/Q1617 Urdu (220-219). Residue: Q1615 Neil Armstrong 219, Q567 Angela Merkel 219.
- Landing: base 5aa00355, batch b3857264 (61 insertions, maps 2708→2718, urls 1403→1413), merge db6c8c82 after #933 (+2 NSF) auto-merged clean (multiset branch−main == exactly my 10), gate 10/10 byte-sizes == own fetches on two bases, evidence 2703→2713→2715 exit 0, PR #938 OPEN MERGEABLE, body read-back verified.
- Next run: residue-first Q1615/Q567 (re-verify live), then walk tmp-sl-ranked-20260910-sc3.json from rank 1474; paintings Q12418/Q45585 still carried. Fresh QLever discovery query DUE (ranking tail cuts 176, walk band descending through 219).

## 2026-09-09 run d2 (PR #946) addenda

- FRESH DISCOVERY QUERY BURNED (20th ranking): WDQS full-scan 504 (24B body) → QLever GET-with-curl-L answered 200 first-try, 3000 rows, head Q136746190 935, tail cuts at 176. Saved tmp-sl-ranked-20260909-d.json in the worktree ({rows, source, ts} shape). One-query budget spent.
- Junk head runs rank 0-~1310 almost solid: day-of-year Q14795564 wall (256 p31 refusals, the 233-band) + 555 colon-junk + 678 cfg exclusions over the walk to rank 1500; first KEEP region only at ranks ~1312+ (year entities Q1986 2003 / Q49625 2022 at 223, then Hindi 222, Adam Smith 221, then the 219 band). 0 fetchfail; wbgetentities batch triage (50/chunk, fail-open to EntityData) worked first-try.
- PR sweep: only #943/#942/#940 open at sweep (12 junk-QID claims, zero overlap); #934 (class-scout desert batch) MERGED mid-run; open PRs 0 at land time. Residue-first 2/2 verified live: Q1615 Neil Armstrong 219, Q567 Angela Merkel 219. Paintings Q12418 Mona Lisa 146 / Q45585 Starry Night 76 carried a FOURTH time — unclaimed, still below the walk band.
- NEW dropped-map data point: #934's PR diff claims Q8514 but the QID is ABSENT from live main config post-merge (git show grep) — walker independently re-picked Q8514 as KEEP at rank 1499. It was left as residue (below Q612 by rank tie-break) rather than a pick; next run should re-grep live config FIRST and re-propose Q8514 (219) + Q11035 telephone (218) if still absent.
- Picks (EntityData-verified live, all clean P31/colon): Q1986 2003 223, Q49625 2022 223, Q1568 Hindi 222, Q9381 Adam Smith 221, Q1615 Neil Armstrong 219, Q567 Angela Merkel 219, Q2868 Lima 219, Q8442 Otto von Bismarck 219, Q9176 Korean 219, Q612 Dubai 219. Year entities P31 Q3186692 ride the band per #787 precedent.
- Land cycle: main moved 4c8316fa→20232c0b→67b1cdc8→adb3b98d between bootstrap/gate/land; one-shot land cycle-loop handled it, landed first cycle after the last move at adb3b98d. Gate 10/10 exit 0 (byte sizes == own curl fetches 10/10); evidence 2739 base → 2749 branch (+10 exact) exit 0, per-id greps 2x each; commit 3b6a5afc config-only numstat 62/1 (minus line = benign re-aligned top-level `}` under the bare-] shape — do NOT hardcode 61/0 for this shape, assert the minus line is `}`); ls-remote==HEAD; PR #946 OPEN MERGEABLE/CLEAN; body read-back 10/10 ids + GATE_EXIT=0 + 2749.
- Recovable-abort chain held again: land rc3'd on the 61/0 numstat AFTER commit — recovery script (verify HEAD/branch/parent==origin/main, committed-tail geometry, evidence, push, PR create) completed the landing without re-splicing. Self-inflicted recov bug: asserted `ne[-2]==''` on a blank-FILTERED list — never assert blank lines on a blank-stripped list; strip() compare instead.
- Next run: residue-first Q8514 desert 219 + Q11035 telephone 218 (RE-GREP live config — #934 dropped-map suspect) + paintings Q12418/Q45585 (146/76), then walk tmp-sl-ranked-20260909-d.json from rank 1501. Ranking tail cuts at 176 — fresh discovery query due within ~1-2 runs.



